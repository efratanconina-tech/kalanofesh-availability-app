# חיבור Supabase - השלב הבא

כרגע האפליקציה עובדת מקומית.

כדי שאת ובעלך תראו את אותם נתונים משני טלפונים, צריך Supabase.

## מה צריך ממך עכשיו

משימה 1 בלבד:

לפתוח חשבון Supabase.

קישור:

```text
https://supabase.com
```

מה לבחור:

- אפשר להתחבר עם GitHub או אימייל.
- אין צורך לשלם בשלב הראשון.
- להתחיל ב-Free plan.

אחרי שיהיה חשבון, המשימה הבאה תהיה לפתוח Project חדש ולקבל שני פרטים:

- Project URL
- anon public key

Project URL שכבר הוגדר בפרויקט:

```text
https://vwtwnwpiieisydvvamqi.supabase.co
```

Publishable key הוגדר מקומית בקובץ `.env.local`.

השלב הבא:

```text
יצירת טבלאות במסד הנתונים
```

## מה נקנה בהמשך

לא עכשיו, אבל בהמשך כנראה צריך:

- דומיין/סאב-דומיין לאפליקציה, למשל `admin.your-domain.co.il`.
- אחסון סטטי, למשל Vercel או Netlify.
- אולי Supabase בתשלום רק אם יהיה שימוש גבוה או הרבה עובדים.

בשלב הזה לא לקנות כלום.
